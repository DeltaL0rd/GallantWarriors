using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScreenPoint : MonoBehaviour
{
    /*public RectTransform parent;
    public RectTransform target;
    private Camera cam;

    void Start()
    {
        cam = FindObjectOfType<Camera>();
    }

    void Update()
    {
        Vector2 anchredPos;
        RectTransformUtility.ScreenPointToLocalPointInRectangle(parent, Input.mousePosition, null, out anchredPos);
        target.anchoredPosition = anchredPos;
    }*/

    /*private void Update()
    {
        if (Input.touchCount > 0)
        {
            if (Input.GetTouch(0).position.x < Screen.width / 2)
            {
                Debug.Log("Touched Left Side");
            }
            else
            {
                Debug.Log("Touched Right Side");
            }
        }
    }*/
}
